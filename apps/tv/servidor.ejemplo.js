/**
 * Dirección del servidor de sincronización.
 *
 * Este fichero es la plantilla y va en el repositorio, que es público. Para
 * apuntar a tu servidor, copia esto a `servidor.local.js` y pon la dirección
 * ahí: Metro coge el local si existe, y ese no se sube.
 *
 *   cp apps/tv/servidor.ejemplo.js apps/tv/servidor.local.js
 *
 * En blanco, la app pide la dirección a mano al emparejar.
 */
module.exports = { SERVIDOR: '' };
