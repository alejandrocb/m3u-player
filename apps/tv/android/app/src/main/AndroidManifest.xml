<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Para seguir bajando con la aplicación en segundo plano. El tipo es
      `dataSync` porque lo que hace es traer datos de la red, y desde Android
      14 cada tipo pide su propio permiso.

      `POST_NOTIFICATIONS` es para que el aviso se vea: desde Android 13 hay
      que pedirlo. Si se deniega, el servicio funciona igual y la descarga
      sigue; lo único que pasa es que no se ve por dónde va.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      El `PARTIAL_WAKE_LOCK` que coge `HeadlessJsTaskService`: con la pantalla
      apagada el aparato se suspende, y suspendido no baja nada.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
      Para encontrar las teles de casa ("Ver en la tele"). Muchos Android tiran
      los paquetes multicast para ahorrar batería, y sin este permiso la
      búsqueda vuelve vacía aunque la tele esté encendida al lado.
    -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!--
      "Ver en la tele" pasa el vídeo por el teléfono, así que mientras dura el
      teléfono tiene que seguir vivo aunque se salga de la aplicación. Es un
      servicio de reproducción: lo que suena, suena en otra pantalla.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!--
      Para poder **preguntar** si se nos deja seguir con la pantalla apagada.
      La ventana la enseña el sistema y la contesta quien mira: sin esto, el
      teléfono congela la aplicación a los pocos minutos de bloquear y la tele
      se queda parada a media película.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      Instalar una actualizacion desde la propia aplicacion.

      Hasta ahora cada version entraba por `adb`, con el portatil y el cable:
      en una tablet es incomodo y en la tele del salon es un viaje, asi que los
      aparatos se quedaban atras unos de otros. El APK lo reparte el servidor
      de la casa por su API con token, no hay ninguna direccion abierta.

      Instala **el sistema**, no nosotros: esto solo permite pedirselo. Ademas
      hay que darle permiso de "instalar aplicaciones desconocidas", una vez
      por aparato, y Android ensena que va a instalar antes de hacerlo.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
 
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:banner="@drawable/tv_banner"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:networkSecurityConfig="@xml/network_security_config"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent-filter>
      </activity>

      <service
        android:name=".ServicioDeDescargas"
        android:foregroundServiceType="dataSync"
        android:exported="false" />

      <service
        android:name=".ServicioDeTele"
        android:foregroundServiceType="mediaPlayback"
        android:exported="false" />
    </application>
</manifest>
